apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mental_health.fullname" . }}-test-connection"
  labels:
    {{- include "mental_health.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mental_health.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
