apiVersion: v1
kind: Service
metadata:
  name: {{ include "mental_health.fullname" . }}
  labels:
    {{- include "mental_health.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mental_health.selectorLabels" . | nindent 4 }}
